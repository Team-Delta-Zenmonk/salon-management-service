User selects services
        ↓
Cart is created (services + optional staff)
        ↓
Availability API is called
        ↓
System computes POSSIBLE SLOTS (read-only)
        ↓
User selects a slot
        ↓
Slot is TEMPORARILY LOCKED
        ↓
User confirms booking
        ↓
Booking is FINALIZED (atomic DB transaction)







STEP 1 → Normalize cart into service chains
STEP 2 → Build staff availability calendars
STEP 3 → Single-service slot engine
STEP 4 → Multi-service sequential engine
STEP 5 → Group booking engine
STEP 6 → Staff assignment (fixed + random)
